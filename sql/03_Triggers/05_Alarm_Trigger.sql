CREATE TRIGGER TR_Alarm_CreateWorkOrder
ON Alarm
AFTER INSERT
AS
BEGIN
    SET NOCOUNT ON;

    INSERT INTO WorkOrder(
        AlarmId, MaintainerId, DispatchTime
    )
    SELECT
        i.AlarmId,
        1,                 -- ??§Õ???????????ID?????????????????????????
        SYSDATETIME()
    FROM inserted i
    WHERE i.AlarmLevel = N'¸ß';
END;
GO